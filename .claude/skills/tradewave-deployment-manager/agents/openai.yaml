interface:
  display_name: "TradeWave Deployment Manager"
  short_description: "Autonomous gated staging releases"
  default_prompt: "Use $tradewave-deployment-manager to deploy the approved dev behavior to staging autonomously and stop before production."
policy:
  allow_implicit_invocation: true
